import { Component } from 'react';
import { type Props, defaultProps } from '../../packages/react-select/src/stateManager';

export default class StateManager extends Component<Props> {
  defaultProps = defaultProps;
}
